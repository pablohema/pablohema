fun main(){
    var l = mutableListOf<Int>( 1 , 2 , 3 , 4 , 3)
    val s = mutableSetOf<Int>( 1 , 2 , 3 , 4 , 3)
    var m = mutableMapOf("Box" to 12, "Books" to 18, "Table" to 13)

    l.size
    s.size
    m.size

    l.clear()
    s.clear()
    m.clear()

    l.subList()
    s.subList()
    m.subList()

    l.isEmpty()
    s.isEmpty()
    m.isEmpty()

    l.add(1)
    s.add(2)


}